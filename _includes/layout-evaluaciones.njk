<!DOCTYPE html>
<html lang="es" class="scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }} - Francisco Javier Núñez Valenzuela</title>
    
    <meta name="description" content="Recursos educativos y herramientas de práctica para docentes">
    
    <!-- AI Assistant Meta Tags (if page has dominio-lenguaje) -->
    {% if page.data["dominio-lenguaje"] %}
    <meta name="dominio-lenguaje" content="{{ page.data['dominio-lenguaje'] }}">
    <meta name="contenido-resumen" content="{{ page.data['contenido-resumen'] }}">
    {% endif %}
    
    <!-- Theme & social preview -->
    <meta name="theme-color" content="#2563eb">
    <meta property="og:title" content="{{ title }} - Francisco Javier Núñez Valenzuela">
    <meta property="og:type" content="website">
    <meta property="og:url" content="https://profefranciscopancho.com/">
    <meta property="og:image" content="https://profefranciscopancho.com/imagenes/logo-profe-francisco-nuevo.svg">

    <!-- Favicons -->
    <link rel="apple-touch-icon" href="/imagenes/logo-profe-francisco-nuevo.svg">
    <link rel="icon" href="/imagenes/logo-profe-francisco-nuevo.svg" type="image/svg+xml" sizes="any">
    <link rel="manifest" href="/site.webmanifest">
    
    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@700;900&family=Inter:wght@400;500;600;700&family=Poppins:wght@300;400;600;700;800&display=swap" rel="stylesheet">
    
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    
    <!-- Font Awesome Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" integrity="sha512-iecdLmaskl7CVkqkXNQ/ZH/XLlvWZOJyj7Yy7tcenmpD1ypASozpmT/E0iPtmFIB46ZmdtAc9eNBvH0H/ZpiBw==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    
    <!-- Styles -->
    <link rel="stylesheet" href="/css/tw.css">
    <link rel="stylesheet" href="/css/design-tokens.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/premium-styles.css">
    <link rel="stylesheet" href="/css/lenguaje-premium.css">

    <!-- MathJax para renderizado de fórmulas matemáticas -->
    <script>
    MathJax = {
      tex: {
        inlineMath: [['$', '$'], ['\\(', '\\)']],
        displayMath: [['$$', '$$'], ['\\[', '\\]']],
        processEscapes: true,
        processEnvironments: true
      },
      options: {
        skipHtmlTags: ['script', 'noscript', 'style', 'textarea', 'pre']
      }
    };
    </script>
    <script id="MathJax-script" async src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-mml-chtml.js"></script>

    {# Allow pages to inject head content #}
    {% if head is defined %}
        {{ head | safe }}
    {% endif %}
</head>
<body class="bg-gray-50 text-gray-900 transition-colors duration-300">

    <!-- Contenido directo sin navbar -->
    <main>
        {{ content | safe }}
    </main>

    <!-- Footer minimalista -->
    <footer class="bg-white border-t border-gray-200 mt-12">
        <div class="container mx-auto px-6 py-6 text-center">
            <p class="text-sm text-gray-600">© 2025 Francisco Javier Núñez Valenzuela. Profesor de Lengua y Literatura.</p>
            <div class="mt-2 space-x-4">
                <a href="/" class="text-sm text-blue-600 hover:underline">← Volver al sitio principal</a>
            </div>
        </div>
    </footer>

    <!-- Firebase SDK -->
    <script src="https://www.gstatic.com/firebasejs/9.22.0/firebase-app-compat.js"></script>
    <script src="https://www.gstatic.com/firebasejs/9.22.0/firebase-auth-compat.js"></script>
    <script src="https://www.gstatic.com/firebasejs/9.22.0/firebase-database-compat.js"></script>

    <!-- Auth Protection Script -->
    <script>
    // Firebase Configuration
    const firebaseConfig = {
        apiKey: "AIzaSyCzN4xNEE_hKshXbsVqLhWSnzet1pHwRh8",
        authDomain: "profe-blog.firebaseapp.com",
        databaseURL: "https://profe-blog-default-rtdb.firebaseio.com",
        projectId: "profe-blog",
        storageBucket: "profe-blog.firebasestorage.app",
        messagingSenderId: "305920739217",
        appId: "1:305920739217:web:2e08c7469d2988d8b3bc30"
    };

    // Initialize Firebase
    if (!firebase.apps.length) {
        firebase.initializeApp(firebaseConfig);
    }

    // Wait for page to be ready before checking authentication
    window.addEventListener('DOMContentLoaded', function() {
        const currentPath = window.location.pathname;
        const isLoginPage = currentPath === '/evaluaciones/' || currentPath === '/evaluaciones/index.html';
        const isAdminLogin = currentPath.includes('/evaluaciones/admin/login');
        const isDossierPage = currentPath.includes('/dossier-lenguaje-media/') || 
                              currentPath.includes('/dossier-matematica-media/');
        
        // Don't block login pages or public dossier pages
        if (isLoginPage || isAdminLogin || isDossierPage) {
            console.log('✓ Public page - no authentication check');
            return;
        }

        // Check session
        const session = localStorage.getItem('evaluaciones_session');
        
        if (!session) {
            // No session - redirect to login
            console.warn('⚠️ No session found');
            alert('⚠️ Debes iniciar sesión para acceder a este contenido');
            window.location.href = '/evaluaciones/';
            return;
        }

        try {
            const sessionData = JSON.parse(session);
            const loginTime = new Date(sessionData.loginTime);
            const now = new Date();
            const hoursDiff = (now - loginTime) / (1000 * 60 * 60);

            // Session expired (24 hours)
            if (hoursDiff >= 24) {
                console.warn('⏱️ Session expired');
                localStorage.removeItem('evaluaciones_session');
                alert('⏱️ Tu sesión ha expirado. Por favor inicia sesión nuevamente.');
                window.location.href = '/evaluaciones/';
                return;
            }

            // Valid session - check permissions for specific sections
            const permissions = sessionData.permissions || {};
            
            if (currentPath.includes('/educacion-basica/') && !permissions.basica) {
                console.warn('🚫 No permission for Educación Básica');
                alert('🚫 No tienes permisos para acceder a Educación Básica');
                window.location.href = '/evaluaciones/';
                return;
            }
            
            if (currentPath.includes('/educacion-especial/') && !permissions.especial) {
                console.warn('🚫 No permission for Educación Especial');
                alert('🚫 No tienes permisos para acceder a Educación Especial');
                window.location.href = '/evaluaciones/';
                return;
            }

            if (currentPath.includes('/educacion-media/') && !permissions.media) {
                console.warn('🚫 No permission for Educación Media');
                alert('🚫 No tienes permisos para acceder a Educación Media');
                window.location.href = '/evaluaciones/';
                return;
            }

            // Valid session and permissions - continue
            console.log('✓ Authenticated:', sessionData.username, 'Permissions:', permissions);

        } catch (error) {
            console.error('❌ Authentication error:', error);
            localStorage.removeItem('evaluaciones_session');
            alert('❌ Error de autenticación. Por favor inicia sesión nuevamente.');
            window.location.href = '/evaluaciones/';
        }
    });
    </script>

    <!-- Bootstrap 5 JS Bundle (includes Popper) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>

    <!-- Error Tracker - Sistema de rastreo de errores mejorado -->
    <script src="/js/error-tracker.js"></script>

    <!-- Scripts -->
    <script src="/main-premium.js"></script>
    
    <!-- AI Assistant -->
    <link rel="stylesheet" href="/css/ai-assistant.css">
    <script src="/js/ai-assistant-lenguaje.js"></script>
    
    {# Allow pages to inject scripts #}
    {% if scripts is defined %}
        {{ scripts | safe }}
    {% endif %}
</body>
</html>
