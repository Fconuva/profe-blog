---
layout: layout.njk
title: "Blog de un Profesor"
---

<div class="max-w-3xl mx-auto">
    <h1 class="text-4xl font-black text-blue-700 mb-8">{{ title }}</h1>
    
    <div class="space-y-12">
        {% for post in contentful %}
            <article>
                <h2 class="text-3xl font-bold mb-2">
                    <a href="/blog/{{ post.titulo | slug }}/" class="text-gray-800 hover:text-blue-700">{{ post.titulo }}</a>
                </h2>
                <p class="text-gray-500 mb-4">{{ post.fecha | readableDate }}</p>
                {% if post.imagenPrincipal %}
                    <img src="https://{{ post.imagenPrincipal.fields.file.url }}" alt="{{ post.imagenPrincipal.fields.description }}" class="rounded-lg mb-4">
                {% endif %}
                <div class="prose max-w-none">
                    {% documentToHtml post.contenido %}
                </div>
            </article>
        {% endfor %}
    </div>
</div>
