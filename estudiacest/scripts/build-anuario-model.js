const fs = require('node:fs');
const path = require('node:path');
const root = path.resolve(__dirname,'..');
const pages = require('../4dtp/modelo-content.js');
const esc = value => String(value||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const html = `<!doctype html><html lang="es"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Modelo completo resuelto · Anuario 4°D TP 2026</title><meta name="description" content="Anuario didáctico completo de 24 páginas: portada, cinco entrevistas, nueve secciones, despedida y contraportada. Textos ficticios e ilustraciones con IA."><link rel="stylesheet" href="modelo.css"></head><body>
<header class="model-toolbar"><a href="/4dtp/">← Volver al anuario</a><label for="pageSelect">Ir a una parte</label><select id="pageSelect">${pages.map((p,i)=>`<option value="${p.id}">${String(i+1).padStart(2,'0')} · ${esc(p.title)}</option>`).join('')}</select><a href="modelo-completo.pdf" download>Descargar PDF · 24 páginas</a><button type="button" id="printModel">Imprimir</button></header>
<aside class="model-notice"><strong>Modelo completo resuelto · 24 páginas</strong><p>Todos los personajes, entrevistas y recuerdos son ficticios. Las tres ilustraciones se generaron con IA y no muestran a estudiantes ni docentes reales. Úsalo para comprender la estructura; escribe tu propia historia. Las cinco entrevistas del ejemplo no tienen audios porque son simuladas.</p></aside>
<main>${pages.map((p,i)=>`<article class="book-page ${p.cover?'cover':''} ${p.back?'back-cover':''}" id="${p.id}"><header><span class="kicker">${esc(p.kicker||'Huellas en papel · Edición didáctica')}</span><h1>${esc(p.title)}</h1></header>${p.image?`<figure><img src="${esc(p.image)}" alt="${esc(p.caption||'Ilustración ficticia generada con IA para el modelo del anuario')}" ${i>0?'loading="lazy"':''}><figcaption>${esc(p.caption||'Ilustración generada con IA · Modelo ficticio')}</figcaption></figure>`:''}<div class="page-text">${p.text.split('\n\n').map(t=>`<p>${esc(t).replace(/\n/g,'<br>')}</p>`).join('')}</div>${p.secondImage?`<figure class="secondary-image"><img src="${esc(p.secondImage)}" alt="Segunda ilustración ficticia de la galería, generada con IA" loading="lazy"></figure>`:''}<footer><span>4°D TP · CEST · 2026 · Modelo ficticio</span><b>${String(i+1).padStart(2,'0')}</b></footer></article>`).join('\n')}</main>
<script>document.getElementById('pageSelect').addEventListener('change',event=>{location.hash=event.target.value;});document.getElementById('printModel').addEventListener('click',()=>window.print());</script></body></html>`;
fs.writeFileSync(path.join(root,'4dtp/modelo.html'),html);
console.log(`Modelo resuelto generado: ${pages.length} páginas.`);
