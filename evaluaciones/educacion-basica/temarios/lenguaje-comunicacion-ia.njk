---
layout: layout-evaluaciones.njk
title: Lenguaje y Comunicación - Práctica con IA
description: Práctica interactiva de Lenguaje y Comunicación con IA integrada
---

<div class="min-h-screen bg-gradient-to-br from-purple-50 to-pink-100">
  <div class="container mx-auto px-4 py-8">
    <!-- Header -->
    <div class="text-center mb-8">
      <h1 class="text-4xl font-bold text-gray-800 mb-4">
        🤖 Lenguaje y Comunicación - Práctica con IA
      </h1>
      <p class="text-xl text-gray-600 max-w-3xl mx-auto mb-4">
        Entrenamiento personalizado con Google Gemini 2.5 para preparación ECEP
      </p>
      <div class="flex justify-center space-x-4">
        <a href="/evaluaciones/educacion-basica/temarios/lenguaje-comunicacion/" class="text-purple-600 hover:text-purple-800 font-medium">
          ← Volver al Temario
        </a>
        <a href="/evaluaciones/educacion-basica/pruebas/63-sc-l/" class="bg-purple-500 hover:bg-purple-600 text-white px-4 py-2 rounded-lg font-medium">
          📝 Ir a Prueba Oficial
        </a>
      </div>
    </div>

    <!-- Estado de la IA -->
    <div class="bg-white rounded-xl shadow-lg p-6 mb-8 max-w-4xl mx-auto">
      <div class="flex items-center justify-between">
        <div class="flex items-center">
          <div class="w-4 h-4 bg-green-500 rounded-full mr-3 animate-pulse"></div>
          <div>
            <h3 class="font-semibold text-gray-800">Google Gemini 2.5 - Conectado</h3>
            <p class="text-sm text-gray-600">IA especializada en currículum chileno</p>
          </div>
        </div>
        <div class="text-right">
          <div class="text-sm text-gray-500">Versión 2024.1</div>
          <div class="text-xs text-gray-400">Actualizado: Dic 2024</div>
        </div>
      </div>
    </div>

    <!-- Panel Principal -->
    <div class="grid grid-cols-1 lg:grid-cols-3 gap-8 max-w-7xl mx-auto">

      <!-- Panel de Control -->
      <div class="lg:col-span-1">
        <div class="bg-white rounded-xl shadow-lg p-6 sticky top-8">
          <h3 class="text-xl font-bold text-gray-800 mb-4">🎯 Modos de Práctica</h3>
          <div class="space-y-3">
            <button id="btn-explicacion" class="w-full bg-purple-500 hover:bg-purple-600 text-white font-medium py-3 px-4 rounded-lg transition-colors flex items-center">
              <span class="mr-2">💬</span>
              Explicación Personalizada
            </button>
            <button id="btn-ejercicio" class="w-full bg-blue-500 hover:bg-blue-600 text-white font-medium py-3 px-4 rounded-lg transition-colors flex items-center">
              <span class="mr-2">📝</span>
              Generar Ejercicio
            </button>
            <button id="btn-simulacro" class="w-full bg-green-500 hover:bg-green-600 text-white font-medium py-3 px-4 rounded-lg transition-colors flex items-center">
              <span class="mr-2">🎯</span>
              Simulacro Adaptativo
            </button>
            <button id="btn-casos" class="w-full bg-orange-500 hover:bg-orange-600 text-white font-medium py-3 px-4 rounded-lg transition-colors flex items-center">
              <span class="mr-2">📚</span>
              Casos de Estudio
            </button>
          </div>

          <!-- Configuración -->
          <div class="mt-6 pt-6 border-t border-gray-200">
            <h4 class="font-semibold text-gray-800 mb-3">⚙️ Configuración</h4>
            <div class="space-y-3">
              <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Nivel de Complejidad</label>
                <select id="nivel-dificultad" class="w-full border border-gray-300 rounded-lg px-3 py-2">
                  <option value="basico">Básico (1°-3° básico)</option>
                  <option value="intermedio">Intermedio (4°-5° básico)</option>
                  <option value="avanzado">Avanzado (6° básico)</option>
                </select>
              </div>
              <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Tipo de Contenido</label>
                <select id="tipo-contenido" class="w-full border border-gray-300 rounded-lg px-3 py-2">
                  <option value="comprension">Comprensión Lectora</option>
                  <option value="escritura">Producción de Textos</option>
                  <option value="oralidad">Oralidad</option>
                  <option value="mixto">Mixto</option>
                </select>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Área de Trabajo -->
      <div class="lg:col-span-2">
        <div class="bg-white rounded-xl shadow-lg p-6 min-h-[600px]">
          <div id="work-area" class="space-y-6">
            <!-- Mensaje de bienvenida -->
            <div class="text-center py-12">
              <div class="text-6xl mb-4">🤖</div>
              <h3 class="text-2xl font-bold text-gray-800 mb-4">¡Bienvenido a tu Asistente IA!</h3>
              <p class="text-gray-600 max-w-2xl mx-auto">
                Selecciona un modo de práctica para comenzar. La IA está especializada en el currículum
                nacional de Lenguaje y Comunicación y puede generar explicaciones personalizadas,
                ejercicios adaptativos y retroalimentación detallada.
              </p>
              <div class="mt-6 grid grid-cols-1 md:grid-cols-3 gap-4">
                <div class="bg-purple-50 p-4 rounded-lg">
                  <div class="text-2xl mb-2">📚</div>
                  <div class="font-semibold text-purple-800">Conoce el Currículum</div>
                  <div class="text-sm text-purple-600">Bases 2023 actualizadas</div>
                </div>
                <div class="bg-blue-50 p-4 rounded-lg">
                  <div class="text-2xl mb-2">🎯</div>
                  <div class="font-semibold text-blue-800">Ejercicios Adaptativos</div>
                  <div class="text-sm text-blue-600">Según tu nivel</div>
                </div>
                <div class="bg-green-50 p-4 rounded-lg">
                  <div class="text-2xl mb-2">📊</div>
                  <div class="font-semibold text-green-800">Retroalimentación IA</div>
                  <div class="text-sm text-green-600">Corrección inteligente</div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Historial de Sesiones -->
    <div class="mt-8 bg-white rounded-xl shadow-lg p-6 max-w-7xl mx-auto">
      <h3 class="text-xl font-bold text-gray-800 mb-4">📈 Historial de Práctica</h3>
      <div class="overflow-x-auto">
        <table class="min-w-full bg-white border border-gray-300">
          <thead>
            <tr class="bg-gray-50">
              <th class="border border-gray-300 px-4 py-2 text-left">Fecha</th>
              <th class="border border-gray-300 px-4 py-2 text-left">Tipo</th>
              <th class="border border-gray-300 px-4 py-2 text-left">Tema</th>
              <th class="border border-gray-300 px-4 py-2 text-center">Puntuación</th>
              <th class="border border-gray-300 px-4 py-2 text-center">Acciones</th>
            </tr>
          </thead>
          <tbody id="historial-body">
            <tr>
              <td colspan="5" class="border border-gray-300 px-4 py-8 text-center text-gray-500">
                No hay sesiones registradas aún. ¡Comienza practicando!
              </td>
            </tr>
          </tbody>
        </table>
      </div>
    </div>

  </div>
</div>

<!-- Scripts para la funcionalidad de IA -->
<script>
// Configuración de Gemini API
const GEMINI_CONFIG = {
  apiKey: 'AIzaSyDUMMY_KEY_FOR_DEMO', // Se reemplazará con la clave real
  model: 'gemini-2.0-flash-exp',
  baseURL: 'https://generativelanguage.googleapis.com/v1beta'
};

// Estado de la aplicación
let currentMode = null;
let currentExercise = null;

// Funciones de utilidad
function showLoading() {
  const workArea = document.getElementById('work-area');
  workArea.innerHTML = `
    <div class="text-center py-12">
      <div class="animate-spin rounded-full h-12 w-12 border-b-2 border-purple-500 mx-auto mb-4"></div>
      <p class="text-gray-600">Generando contenido con IA...</p>
    </div>
  `;
}

function showError(message) {
  const workArea = document.getElementById('work-area');
  workArea.innerHTML = `
    <div class="bg-red-50 border border-red-200 rounded-lg p-6 text-center">
      <div class="text-red-500 text-4xl mb-4">⚠️</div>
      <h3 class="text-lg font-semibold text-red-800 mb-2">Error</h3>
      <p class="text-red-600">${message}</p>
      <button onclick="location.reload()" class="mt-4 bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded-lg">
        Reintentar
      </button>
    </div>
  `;
}

// Funciones de IA
async function generateExplanation(topic) {
  showLoading();

  const prompt = `Como experto en pedagogía chilena especializado en Lenguaje y Comunicación,
  explica el siguiente tema de manera clara y didáctica para profesores de educación básica:

  Tema: ${topic}

  Estructura la explicación considerando:
  1. Definición clara
  2. Importancia en el currículum nacional
  3. Ejemplos prácticos para el aula
  4. Estrategias de enseñanza
  5. Criterios de evaluación

  Usa un lenguaje accesible y relaciona con las Bases Curriculares 2023.`;

  try {
    const response = await callGeminiAPI(prompt);
    displayExplanation(response);
  } catch (error) {
    showError('Error al generar explicación. Inténtalo nuevamente.');
  }
}

async function generateExercise(type, level) {
  showLoading();

  const prompt = `Genera un ejercicio de Lenguaje y Comunicación para educación básica
  siguiendo el currículum nacional chileno.

  Tipo de ejercicio: ${type}
  Nivel: ${level}

  El ejercicio debe incluir:
  1. Contexto o texto base
  2. Pregunta(s) clara(s)
  3. Opciones de respuesta (si aplica)
  4. Explicación de la respuesta correcta
  5. Relación con objetivos de aprendizaje

  Asegúrate de que sea apropiado para el nivel educativo especificado.`;

  try {
    const response = await callGeminiAPI(prompt);
    displayExercise(response);
  } catch (error) {
    showError('Error al generar ejercicio. Inténtalo nuevamente.');
  }
}

async function callGeminiAPI(prompt) {
  // Simulación de llamada a API (en producción usar la clave real)
  return new Promise((resolve) => {
    setTimeout(() => {
      resolve({
        content: `Esta es una respuesta simulada de Gemini API para el prompt: "${prompt.substring(0, 100)}..."

En una implementación real, aquí se haría la llamada a la API de Google Gemini con la clave configurada.`,
        timestamp: new Date().toISOString()
      });
    }, 2000);
  });
}

// Funciones de display
function displayExplanation(data) {
  const workArea = document.getElementById('work-area');
  workArea.innerHTML = `
    <div class="space-y-6">
      <div class="flex items-center justify-between">
        <h3 class="text-2xl font-bold text-gray-800">💬 Explicación Personalizada</h3>
        <span class="text-sm text-gray-500">Generado por IA</span>
      </div>

      <div class="bg-purple-50 border-l-4 border-purple-400 p-6 rounded-lg">
        <div class="prose prose-lg max-w-none">
          <p class="text-gray-700 mb-4">${data.content}</p>
        </div>
      </div>

      <div class="flex space-x-4">
        <button onclick="generateExplanation('${currentMode}')" class="bg-purple-500 hover:bg-purple-600 text-white px-4 py-2 rounded-lg">
          🔄 Generar Nueva Explicación
        </button>
        <button onclick="generateExercise('comprension', 'intermedio')" class="bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded-lg">
          📝 Crear Ejercicio Relacionado
        </button>
      </div>
    </div>
  `;
}

function displayExercise(data) {
  const workArea = document.getElementById('work-area');
  workArea.innerHTML = `
    <div class="space-y-6">
      <div class="flex items-center justify-between">
        <h3 class="text-2xl font-bold text-gray-800">📝 Ejercicio Generado</h3>
        <span class="text-sm text-gray-500">Generado por IA</span>
      </div>

      <div class="bg-blue-50 border-l-4 border-blue-400 p-6 rounded-lg">
        <div class="prose prose-lg max-w-none">
          <p class="text-gray-700 mb-4">${data.content}</p>
        </div>
      </div>

      <div class="bg-white border border-gray-200 rounded-lg p-4">
        <h4 class="font-semibold text-gray-800 mb-3">Tu Respuesta:</h4>
        <textarea id="exercise-answer" class="w-full border border-gray-300 rounded-lg p-3 h-32" placeholder="Escribe tu respuesta aquí..."></textarea>
        <button onclick="submitAnswer()" class="mt-3 bg-green-500 hover:bg-green-600 text-white px-4 py-2 rounded-lg">
          📤 Enviar Respuesta
        </button>
      </div>
    </div>
  `;
}

// Event listeners
document.getElementById('btn-explicacion').addEventListener('click', () => {
  currentMode = 'comprension-lectora';
  generateExplanation('Comprensión Lectora - Estrategias Básicas');
});

document.getElementById('btn-ejercicio').addEventListener('click', () => {
  const nivel = document.getElementById('nivel-dificultad').value;
  const tipo = document.getElementById('tipo-contenido').value;
  generateExercise(tipo, nivel);
});

document.getElementById('btn-simulacro').addEventListener('click', () => {
  showLoading();
  setTimeout(() => {
    const workArea = document.getElementById('work-area');
    workArea.innerHTML = `
      <div class="text-center py-12">
        <div class="text-4xl mb-4">🎯</div>
        <h3 class="text-2xl font-bold text-gray-800 mb-4">Simulacro Adaptativo</h3>
        <p class="text-gray-600 mb-6">Esta funcionalidad estará disponible próximamente con integración completa de Gemini API.</p>
        <button onclick="location.reload()" class="bg-purple-500 hover:bg-purple-600 text-white px-6 py-3 rounded-lg">
          Volver al Inicio
        </button>
      </div>
    `;
  }, 1000);
});

document.getElementById('btn-casos').addEventListener('click', () => {
  showLoading();
  setTimeout(() => {
    const workArea = document.getElementById('work-area');
    workArea.innerHTML = `
      <div class="text-center py-12">
        <div class="text-4xl mb-4">📚</div>
        <h3 class="text-2xl font-bold text-gray-800 mb-4">Casos de Estudio</h3>
        <p class="text-gray-600 mb-6">Análisis de situaciones reales en el aula con soluciones basadas en evidencia.</p>
        <button onclick="location.reload()" class="bg-purple-500 hover:bg-purple-600 text-white px-6 py-3 rounded-lg">
          Volver al Inicio
        </button>
      </div>
    `;
  }, 1000);
});

// Función para enviar respuesta (placeholder)
function submitAnswer() {
  const answer = document.getElementById('exercise-answer').value;
  if (!answer.trim()) {
    alert('Por favor escribe una respuesta antes de enviar.');
    return;
  }

  alert('Respuesta enviada. La retroalimentación de IA estará disponible próximamente.');
}
</script>
