---
layout: layout.njk
title: Temarios de Estudios y Pruebas
description: Temarios, guías de estudio y pruebas de práctica para Educación Básica
---

<div class="min-h-screen bg-gradient-to-br from-blue-50 via-indigo-50 to-purple-50">
  <div class="container mx-auto px-4 py-12">
    <!-- Login Modal Centrado -->
    <div id="login-modal" class="fixed inset-0 bg-black/50 backdrop-blur-sm hidden z-50 flex items-center justify-center p-4">
      <div class="bg-white rounded-2xl shadow-2xl max-w-md w-full p-8 relative">
        <button onclick="closeLoginModal()" class="absolute top-4 right-4 text-gray-400 hover:text-gray-600">
          <i class="fas fa-times text-xl"></i>
        </button>

        <div class="text-center mb-8">
          <div class="w-16 h-16 bg-gradient-to-br from-indigo-500 to-purple-600 rounded-full flex items-center justify-center mx-auto mb-4">
            <i class="fas fa-user-lock text-white text-2xl"></i>
          </div>
          <h2 class="text-2xl font-bold text-gray-800">Iniciar Sesión</h2>
          <p class="text-gray-600 mt-2">Accede a tu cuenta para continuar</p>
        </div>

        <form id="user-login-form" class="space-y-6">
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">Usuario</label>
            <input type="text" id="login-username" placeholder="Ingresa tu usuario"
                   class="w-full px-4 py-3 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent transition-all" required>
          </div>
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">Contraseña</label>
            <input type="password" id="login-password" placeholder="Ingresa tu contraseña"
                   class="w-full px-4 py-3 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent transition-all" required>
          </div>
          <button type="submit" class="w-full bg-gradient-to-r from-indigo-600 to-purple-600 hover:from-indigo-700 hover:to-purple-700 text-white font-semibold py-3 px-4 rounded-xl transition-all transform hover:scale-105 shadow-lg">
            <i class="fas fa-sign-in-alt mr-2"></i>Iniciar Sesión
          </button>
        </form>

        <div class="text-center mt-6">
          <a href="/evaluaciones/admin/login.html" class="text-indigo-600 hover:text-indigo-800 text-sm font-medium transition-colors">
            <i class="fas fa-cog mr-1"></i>Acceso Administrador
          </a>
        </div>
      </div>
    </div>

    <!-- Hero Section -->
    <div class="text-center mb-16">
      <div class="inline-flex items-center space-x-2 bg-white/60 backdrop-blur-sm px-6 py-3 rounded-full shadow-sm mb-8">
        <i class="fas fa-graduation-cap text-indigo-600"></i>
        <span class="text-sm font-medium text-gray-700">Sistema de Evaluaciones Educativas</span>
      </div>

      <h1 class="text-5xl md:text-6xl font-bold text-gray-800 mb-6 leading-tight">
        📚 <span class="bg-gradient-to-r from-indigo-600 to-purple-600 bg-clip-text text-transparent">Temarios</span> de Estudios
        <br>y <span class="bg-gradient-to-r from-purple-600 to-pink-600 bg-clip-text text-transparent">Pruebas</span>
      </h1>

      <p class="text-xl text-gray-600 max-w-3xl mx-auto mb-8 leading-relaxed">
        Material de estudio completo y práctica interactiva para docentes.
        Preparación especializada para evaluaciones ECEP 2025.
      </p>

      <div id="login-button" class="inline-block">
        <button onclick="showLoginModal()" class="bg-gradient-to-r from-indigo-600 to-purple-600 hover:from-indigo-700 hover:to-purple-700 text-white font-bold py-4 px-8 rounded-2xl transition-all transform hover:scale-105 shadow-xl text-lg">
          <i class="fas fa-play-circle mr-2"></i>Comenzar Ahora
        </button>
      </div>
    </div>

    <!-- Secciones de Contenido -->
    <div class="max-w-7xl mx-auto">
      <!-- 1. Educación Básica -->
      <div id="educacion-basica-section" class="mb-16">
        <div class="text-center mb-12">
          <div class="inline-flex items-center space-x-2 bg-gradient-to-r from-green-100 to-emerald-100 px-6 py-3 rounded-full mb-6">
            <i class="fas fa-school text-green-600"></i>
            <span class="text-sm font-semibold text-green-800">1. EDUCACIÓN BÁSICA</span>
          </div>
          <h2 class="text-4xl font-bold text-gray-800 mb-4">Lenguaje y Comunicación</h2>
          <p class="text-xl text-gray-600 max-w-2xl mx-auto">1° a 6° Básico - Objetivos, contenidos y evaluación</p>
        </div>

        <div class="grid lg:grid-cols-2 gap-8">
          <!-- Estudio de contenidos -->
          <div class="bg-white rounded-2xl shadow-xl overflow-hidden hover:shadow-2xl transition-all duration-300 transform hover:-translate-y-2">
            <div class="bg-gradient-to-r from-indigo-500 to-indigo-600 p-8 text-white">
              <div class="flex items-center space-x-4">
                <div class="w-16 h-16 bg-white/20 rounded-2xl flex items-center justify-center">
                  <i class="fas fa-book-open text-3xl"></i>
                </div>
                <div>
                  <h3 class="text-2xl font-bold">� Estudio de Contenidos</h3>
                  <p class="text-indigo-100 mt-1">Temario completo y objetivos</p>
                </div>
              </div>
            </div>

            <div class="p-8">
              <p class="text-gray-600 mb-6 leading-relaxed">
                Accede a guías didácticas completas con objetivos de aprendizaje,
                explicaciones detalladas, ejemplos contextualizados y casos de estudio
                con apoyo de inteligencia artificial.
              </p>

              <div class="space-y-3 mb-8">
                <div class="flex items-center space-x-3">
                  <i class="fas fa-check-circle text-green-500"></i>
                  <span class="text-gray-700">Objetivos de aprendizaje por nivel</span>
                </div>
                <div class="flex items-center space-x-3">
                  <i class="fas fa-check-circle text-green-500"></i>
                  <span class="text-gray-700">Contenidos curriculares detallados</span>
                </div>
                <div class="flex items-center space-x-3">
                  <i class="fas fa-check-circle text-green-500"></i>
                  <span class="text-gray-700">Ejemplos y casos prácticos</span>
                </div>
                <div class="flex items-center space-x-3">
                  <i class="fas fa-robot text-blue-500"></i>
                  <span class="text-gray-700">Explicaciones con IA pedagógica</span>
                </div>
              </div>

              <a href="/evaluaciones/educacion-basica/estudio/lenguaje-comunicacion/"
                 class="block w-full bg-gradient-to-r from-indigo-600 to-indigo-700 hover:from-indigo-700 hover:to-indigo-800 text-white font-bold py-4 px-6 rounded-xl transition-all text-center shadow-lg hover:shadow-xl">
                <i class="fas fa-arrow-right mr-2"></i>Ir al Estudio Completo
              </a>
            </div>
          </div>

          <!-- Práctica Interactiva -->
          <div class="bg-white rounded-2xl shadow-xl overflow-hidden hover:shadow-2xl transition-all duration-300 transform hover:-translate-y-2">
            <div class="bg-gradient-to-r from-blue-500 to-blue-600 p-8 text-white">
              <div class="flex items-center space-x-4">
                <div class="w-16 h-16 bg-white/20 rounded-2xl flex items-center justify-center">
                  <i class="fas fa-tasks text-3xl"></i>
                </div>
                <div>
                  <h3 class="text-2xl font-bold">📝 Práctica Interactiva</h3>
                  <p class="text-blue-100 mt-1">Evaluación 63-SC-L</p>
                </div>
              </div>
            </div>

            <div class="p-8">
              <p class="text-gray-600 mb-6 leading-relaxed">
                Prueba de práctica completa con 50 ítems de selección múltiple,
                corrección automática y justificación pedagógica generada por IA.
              </p>

              <div class="space-y-3 mb-8">
                <div class="flex items-center space-x-3">
                  <i class="fas fa-check-circle text-green-500"></i>
                  <span class="text-gray-700">50 preguntas tipo ECEP</span>
                </div>
                <div class="flex items-center space-x-3">
                  <i class="fas fa-check-circle text-green-500"></i>
                  <span class="text-gray-700">Corrección automática</span>
                </div>
                <div class="flex items-center space-x-3">
                  <i class="fas fa-check-circle text-green-500"></i>
                  <span class="text-gray-700">Retroalimentación inmediata</span>
                </div>
                <div class="flex items-center space-x-3">
                  <i class="fas fa-robot text-blue-500"></i>
                  <span class="text-gray-700">Explicaciones con IA especializada</span>
                </div>
              </div>

              <a href="/evaluaciones/educacion-basica/pruebas/63-sc-l/practica/"
                 class="block w-full bg-gradient-to-r from-blue-600 to-blue-700 hover:from-blue-700 hover:to-blue-800 text-white font-bold py-4 px-6 rounded-xl transition-all text-center shadow-lg hover:shadow-xl">
                <i class="fas fa-play-circle mr-2"></i>Comenzar Práctica
              </a>
            </div>
          </div>
        </div>
      </div>

      <!-- 2. Educación Especial -->
      <div id="educacion-especial-section" class="mb-16">
        <div class="text-center mb-12">
          <div class="inline-flex items-center space-x-2 bg-gradient-to-r from-purple-100 to-pink-100 px-6 py-3 rounded-full mb-6">
            <i class="fas fa-heart text-purple-600"></i>
            <span class="text-sm font-semibold text-purple-800">2. EDUCACIÓN ESPECIAL</span>
          </div>
          <h2 class="text-4xl font-bold text-gray-800 mb-4">Programa de Integración Escolar (PIE)</h2>
          <p class="text-xl text-gray-600 max-w-2xl mx-auto">Especialidades completas: DEA, TEL, TEA, DI, DM</p>
        </div>

        <div class="bg-white rounded-2xl shadow-xl overflow-hidden hover:shadow-2xl transition-all duration-300">
          <div class="bg-gradient-to-r from-purple-500 via-pink-500 to-rose-500 p-8 text-white">
            <div class="flex items-center justify-between">
              <div class="flex items-center space-x-4">
                <div class="w-16 h-16 bg-white/20 rounded-2xl flex items-center justify-center">
                  <i class="fas fa-users-cog text-3xl"></i>
                </div>
                <div>
                  <h3 class="text-2xl font-bold">🎓 Educación Especial PIE</h3>
                  <p class="text-purple-100 mt-1">Dossier completo + Práctica con IA</p>
                </div>
              </div>
              <div class="text-right">
                <div class="text-2xl font-bold">5</div>
                <div class="text-sm text-purple-100">Especialidades</div>
              </div>
            </div>
          </div>

          <div class="p-8">
            <div class="grid md:grid-cols-2 gap-8 mb-8">
              <div>
                <h4 class="text-lg font-bold text-gray-800 mb-4">📋 Contenido Incluido:</h4>
                <ul class="space-y-2">
                  <li class="flex items-center space-x-3">
                    <i class="fas fa-check-circle text-green-500"></i>
                    <span class="text-gray-700">Tronco común PIE</span>
                  </li>
                  <li class="flex items-center space-x-3">
                    <i class="fas fa-check-circle text-green-500"></i>
                    <span class="text-gray-700">5 especialidades completas</span>
                  </li>
                  <li class="flex items-center space-x-3">
                    <i class="fas fa-check-circle text-green-500"></i>
                    <span class="text-gray-700">Normativa chilena actualizada</span>
                  </li>
                  <li class="flex items-center space-x-3">
                    <i class="fas fa-check-circle text-green-500"></i>
                    <span class="text-gray-700">Estrategias de intervención</span>
                  </li>
                </ul>
              </div>

              <div>
                <h4 class="text-lg font-bold text-gray-800 mb-4">🤖 Características IA:</h4>
                <ul class="space-y-2">
                  <li class="flex items-center space-x-3">
                    <i class="fas fa-robot text-blue-500"></i>
                    <span class="text-gray-700">Chat con Grok especializado</span>
                  </li>
                  <li class="flex items-center space-x-3">
                    <i class="fas fa-brain text-purple-500"></i>
                    <span class="text-gray-700">Explicaciones pedagógicas</span>
                  </li>
                  <li class="flex items-center space-x-3">
                    <i class="fas fa-chart-line text-green-500"></i>
                    <span class="text-gray-700">Evaluación adaptativa</span>
                  </li>
                  <li class="flex items-center space-x-3">
                    <i class="fas fa-comments text-indigo-500"></i>
                    <span class="text-gray-700">Consultas en tiempo real</span>
                  </li>
                </ul>
              </div>
            </div>

            <div class="text-center">
              <a href="/evaluaciones/educacion-especial/"
                 class="inline-block bg-gradient-to-r from-purple-600 to-pink-600 hover:from-purple-700 hover:to-pink-700 text-white font-bold py-4 px-8 rounded-xl transition-all transform hover:scale-105 shadow-lg hover:shadow-xl">
                <i class="fas fa-graduation-cap mr-2"></i>Acceder a Educación Especial
              </a>
            </div>
          </div>
        </div>
      </div>

      <!-- Información del Sistema -->
      <div class="bg-gradient-to-r from-slate-50 to-blue-50 rounded-2xl p-8 border border-slate-200">
        <div class="text-center mb-6">
          <i class="fas fa-info-circle text-3xl text-slate-600 mb-4"></i>
          <h3 class="text-2xl font-bold text-gray-800">Información del Sistema</h3>
        </div>

        <div class="grid md:grid-cols-3 gap-6 mb-6">
          <div class="text-center">
            <div class="w-12 h-12 bg-blue-100 rounded-full flex items-center justify-center mx-auto mb-3">
              <i class="fas fa-shield-alt text-blue-600 text-xl"></i>
            </div>
            <h4 class="font-bold text-gray-800 mb-2">Seguro y Privado</h4>
            <p class="text-gray-600 text-sm">Acceso controlado con autenticación avanzada</p>
          </div>

          <div class="text-center">
            <div class="w-12 h-12 bg-emerald-100 rounded-full flex items-center justify-center mx-auto mb-3">
              <i class="fas fa-robot text-emerald-600 text-xl"></i>
            </div>
            <h4 class="font-bold text-gray-800 mb-2">IA Integrada</h4>
            <p class="text-gray-600 text-sm">Grok con conocimiento especializado en educación</p>
          </div>

          <div class="text-center">
            <div class="w-12 h-12 bg-amber-100 rounded-full flex items-center justify-center mx-auto mb-3">
              <i class="fas fa-sync-alt text-amber-600 text-xl"></i>
            </div>
            <h4 class="font-bold text-gray-800 mb-2">Actualizado</h4>
            <p class="text-gray-600 text-sm">Contenido alineado con ECEP 2025</p>
          </div>
        </div>

        <div class="text-center text-gray-600">
          <p class="mb-2">
            <strong>Disponible actualmente:</strong> Lenguaje y Comunicación (1°-6° Básico) y Educación Especial (PIE)
          </p>
          <p class="text-sm">
            Próximamente: Educación Media, Educación Parvularia y Lengua Indígena
          </p>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- Firebase SDK -->
<script src="https://www.gstatic.com/firebasejs/9.22.0/firebase-app-compat.js"></script>
<script src="https://www.gstatic.com/firebasejs/9.22.0/firebase-database-compat.js"></script>

<script>
// Firebase Configuration
const firebaseConfig = {
    apiKey: "AIzaSyCzN4xNEE_hKshXbsVqLhWSnzet1pHwRh8",
    authDomain: "profe-blog.firebaseapp.com",
    databaseURL: "https://profe-blog-default-rtdb.firebaseio.com",
    projectId: "profe-blog",
    storageBucket: "profe-blog.firebasestorage.app",
    messagingSenderId: "305920739217",
    appId: "1:305920739217:web:2e08c7469d2988d8b3bc30"
};

// Initialize Firebase
if (!firebase.apps.length) {
    firebase.initializeApp(firebaseConfig);
}
const database = firebase.database();

// Global variables
let currentUser = null;

// Check authentication on page load
document.addEventListener('DOMContentLoaded', function() {
    checkAuthentication();
    setupLoginForm();
});

// Check if user is logged in
function checkAuthentication() {
    const session = localStorage.getItem('evaluaciones_session');
    const loginButton = document.getElementById('login-button');

    if (session) {
        try {
            const sessionData = JSON.parse(session);
            // Check if session is still valid (24 hours)
            const loginTime = new Date(sessionData.loginTime);
            const now = new Date();
            const hoursDiff = (now - loginTime) / (1000 * 60 * 60);

            if (hoursDiff < 24) {
                currentUser = sessionData;
                showUserInterface(sessionData);
                filterContentByPermissions(sessionData.permissions);
                return;
            } else {
                localStorage.removeItem('evaluaciones_session');
            }
        } catch (error) {
            localStorage.removeItem('evaluaciones_session');
        }
    }

    // Show login button if not authenticated
    if (loginButton) {
        loginButton.style.display = 'block';
    }
    filterContentByPermissions(null); // Hide all restricted content
}

// Setup login form
function setupLoginForm() {
    const loginForm = document.getElementById('user-login-form');

    loginForm.addEventListener('submit', async function(e) {
        e.preventDefault();

        const username = document.getElementById('login-username').value.trim();
        const password = document.getElementById('login-password').value;

        // Show loading state
        const submitBtn = loginForm.querySelector('button[type="submit"]');
        const originalText = submitBtn.innerHTML;
        submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Verificando...';
        submitBtn.disabled = true;

        try {
            // Find user by username
            const usersRef = database.ref('users');
            const snapshot = await usersRef.orderByChild('username').equalTo(username).once('value');

            if (!snapshot.exists()) {
                throw new Error('Usuario no encontrado');
            }

            const userId = Object.keys(snapshot.val())[0];
            const userData = snapshot.val()[userId];

            // Check if user is active
            if (!userData.active) {
                throw new Error('Cuenta deshabilitada');
            }

            // Verify password
            if (atob(userData.password) !== password) {
                throw new Error('Contraseña incorrecta');
            }

            // Create session
            const sessionData = {
                userId: userId,
                username: userData.username,
                permissions: userData.permissions,
                loginTime: new Date().toISOString()
            };

            localStorage.setItem('evaluaciones_session', JSON.stringify(sessionData));
            currentUser = sessionData;

            // Close modal and show user interface
            closeLoginModal();
            showUserInterface(sessionData);
            filterContentByPermissions(sessionData.permissions);

        } catch (error) {
            alert(error.message || 'Error al iniciar sesión');
        } finally {
            submitBtn.innerHTML = originalText;
            submitBtn.disabled = false;
        }
    });
}

// Show login modal
function showLoginModal() {
    const modal = document.getElementById('login-modal');
    modal.classList.remove('hidden');
    document.body.style.overflow = 'hidden';
}

// Close login modal
function closeLoginModal() {
    const modal = document.getElementById('login-modal');
    modal.classList.add('hidden');
    document.body.style.overflow = 'auto';

    // Clear form
    const form = document.getElementById('user-login-form');
    form.reset();
}

// Show user interface
function showUserInterface(userData) {
    const loginButton = document.getElementById('login-button');
    const userInfo = document.getElementById('user-info');
    const userName = document.getElementById('user-name');

    if (loginButton) loginButton.style.display = 'none';
    if (userInfo) userInfo.classList.remove('hidden');
    if (userName) userName.textContent = userData.username;
}

// Filter content based on permissions
function filterContentByPermissions(permissions) {
    const contentSections = [
        { id: 'educacion-basica-section', permission: 'basica' },
        { id: 'educacion-especial-section', permission: 'especial' },
        { id: 'educacion-media-section', permission: 'media' },
        { id: 'educacion-parvularia-section', permission: 'parvularia' },
        { id: 'lengua-indigena-section', permission: 'indigena' }
    ];

    contentSections.forEach(section => {
        const element = document.getElementById(section.id);
        if (element) {
            if (!permissions || !permissions[section.permission]) {
                // Hide section if no permissions or specific permission not granted
                element.style.display = 'none';
            } else {
                // Show section if permission granted
                element.style.display = 'block';
            }
        }
    });
}

// Logout
function logout() {
    localStorage.removeItem('evaluaciones_session');
    currentUser = null;
    location.reload();
}
</script>